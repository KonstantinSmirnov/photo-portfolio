<div class="row">
  <div id="project-page">
    <div class="slider-for">
      <% @project.photos.each do |photo| %>
        <div>
          <%= image_tag photo.image.url(:cover), class: 'img-fluid slider-cover-photo' %>
        </div>
      <% end %>
    </div>

    <div class="slider-nav">
      <% @project.photos.each do |photo| %>
        <%= image_tag photo.image.url(:thumb) %>
      <% end %>
    </div>
  </div>
</div>
