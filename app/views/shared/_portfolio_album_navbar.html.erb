<nav class="navbar fixed-top navbar-inverse navbar-toggleable-xl">
  <div class="" id="project_navbar">
    <ul class="navbar-nav">
      <li class="nav-item ">
        <%= go_back_link(params[:go_back_page]) %>
      </li>
      <!-- <li class="nav-item">
        <a class="nav-link" href="#">Share</a>
      </li> -->
      <li class="nav-item">
        <a class="nav-link share-button facebook" onclick='share_facebook(
                "<%= "#{project_url(@project)}" %>",
                "<%= "#{@project.title}" %>",
                "<%= "#{URI(request.base_url)}#{@project.photos.first.image.url(:thumb)}" %>",
                "<%= URI(request.base_url) %>",
                "<%= "#{@project.title.gsub("\r\n", " ").gsub('"', '')}" %>"
        )'>
          <i class="fa fa-facebook"></i>
        </a>
      </li>

    </ul>
  </div>
</nav>
