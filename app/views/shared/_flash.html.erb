<% if flash.any? %>
  <div class="flash-alert-wrapper">
    <% flash.each do |key, value| %>
      <div class="flash-alert flash-<%= key %>">
        <%= value %>
      </div>
    <% end %>
  </div>
<% end %>
