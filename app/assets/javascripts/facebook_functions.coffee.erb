jQuery ($) ->
  $(document).on "turbolinks:load", ->
    @share_facebook = (link, title, base_url) ->
      relative_image_url = $('.slider-cover-photo.slick-current img').attr('src')
      cover_image = base_url + relative_image_url
      console.log cover_image
      FB.ui
        method      : 'share'
        href        : link
        title       : title
        picture     : image_url
